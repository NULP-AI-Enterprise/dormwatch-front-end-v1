apiVersion: v1
kind: Service
metadata:
  name: dormwatch-front-v1
  namespace: dormwatch-front-v1
spec:
  type: ClusterIP
  selector:
    app: dormwatch-front-v1
  ports:
    - name: http
      port: 80
      targetPort: http
